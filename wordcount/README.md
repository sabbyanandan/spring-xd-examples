WordCount using Spring XD 'Streams'
===================================

This is an example of a "WordCount" use case built as a custom module that can be complied and packaged as a simple jar file and used in Spring XD runtime environment.
